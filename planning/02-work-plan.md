# Fase 0 — Work Plan

## Secuencia
1. Crear carpeta `legacy/` como contenedor histórico.
2. Mover implementación previa (Apps Script + HTML) a `legacy/`.
3. Mover documentación de auditoría previa a `legacy/planning/`.
4. Crear documentación base en `planning/` para el nuevo ciclo.
5. Registrar cierre de fase en `planning/03-phase-log.md`.

## Criterio de cierre
- La raíz queda enfocada en documentación base y estructura vacía para la siguiente fase.
